REACT_APP_API_URL=http://localhost:5000/api

# Optional: public URL used when generating share links after publishing a survey.
# In production, set this to your deployed frontend URL, e.g.:
#   REACT_APP_PUBLIC_URL=https://your-domain.com
# If not set, the app will fall back to the current origin (window.location.origin).
REACT_APP_PUBLIC_URL=
